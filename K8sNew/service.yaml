kind: Service
apiVersion: v1
metadata:
  name: nginx
spec:
  ports:
    # Accept traffic sent to port 80
    - name: http
      port: 80
      nodePort: 30007
  selector:
    # Loadbalance traffic across Pods matching
    # this label selector
    app: nginx
  type: NodePort